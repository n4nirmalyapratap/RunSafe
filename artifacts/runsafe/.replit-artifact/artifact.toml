kind = "web"
previewPath = "/"
title = "RunSafe"
version = "1.0.0"
id = "artifacts/runsafe"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 18933

[services.development]
run = "pnpm --filter @workspace/runsafe run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/runsafe", "run", "build" ]
publicDir = "artifacts/runsafe/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "18933"
BASE_PATH = "/"
